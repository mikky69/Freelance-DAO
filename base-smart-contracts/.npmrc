# Tells npm to use the flat legacy resolution algorithm.
# This prevents ERESOLVE errors from peer dependency conflicts
# that are technically compatible but npm@7+ rejects by default.
legacy-peer-deps=true